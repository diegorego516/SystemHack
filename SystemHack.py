import scapy

def hackear(os, sys):
    if hackear in os.sys:
        def sistema_alvo(os, sys, scapy):
            if sistema_alvo in os.sys.scapy:
                input("Insira aqui o nome do sistema alvo para hackear: ")
                print("Sistema Hackeado.")
        try:
                print("Seu sistema foi hackeado.")
        except Exception as e:
                print("Você foi hackeado.")